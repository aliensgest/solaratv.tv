# Copy to .env and fill in real values. .env is git-ignored.

# Activation Panel API
ACTIVATION_API_KEY=your_activation_panel_key

# Supabase (server-side — service_role bypasses RLS, never expose to browser)
SUPABASE_URL=https://nuadbjtwcksoqheyushw.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key
